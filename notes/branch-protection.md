# Required checks must exist to pass

A required check that never reports blocks the merge forever rather than failing. Path-filtered workflows are the usual cause, since a skipped workflow reports nothing at all.
