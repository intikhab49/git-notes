# Squash merges and co-authorship

Squashing rewrites the branch into one commit and GitHub assembles the message from the commits. Trailers can be dropped depending on settings, so verify attribution survived if it matters.
