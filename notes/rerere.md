# Reusing recorded conflict resolutions

Enable `rerere.enabled` and git remembers how you resolved a conflict, replaying it automatically next time. Invaluable on long-lived branches that rebase repeatedly against a moving base.
