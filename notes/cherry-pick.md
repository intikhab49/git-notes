# Cherry-picking across branches

`git cherry-pick <sha>` replays one commit elsewhere, creating a new SHA. Use `-x` to append the original hash to the message, which makes backports traceable later.
