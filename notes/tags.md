# Annotated versus lightweight tags

A lightweight tag is just a pointer. An annotated tag (`-a`) is a real object with author, date, and message, and it is what `git describe` and most release tooling expect. Tags are not pushed by default.
