# Dependabot on Actions themselves

Add `package-ecosystem: github-actions` and Dependabot will bump pinned action SHAs. Pin by SHA with the version in a trailing comment so the bot can still read and update it.
