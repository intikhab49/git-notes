# Deploy environments as approval gates

An environment can require a named reviewer before its secrets are released. This is the cleanest way to gate production without giving the deploy job standing access to production credentials.
